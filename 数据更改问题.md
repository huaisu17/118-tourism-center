## 问题

1.采购总成本分析还有随机刷新2.膳食经费数据分析的环形图太大了，与图例重叠了，可以缩小后左移一点3.日管控情况分析的条形图上面还有数据中心的名称和数量等信息没改4.消费数据分析这个图和区域数据分布里大盘走势的图是一样的，你只改了大盘走势里的图，而没改消费数据分析里的图5.食材验收质量分布里的条形图的每条的大小和占比不匹配6.学生营养情况分布的数字要对应在每个不同颜色条形图的中心7.供应商评分分析每个供应商的评分可以改成不一样的值8.月调度请况汇总里的数据并没有给改，改成一月月调度情况，二月月调度情况等等9.师生评价情况分析可以改成鼠标移上去会有例子的10.订餐数据分析的条形图也可以改一下，改成有交互效果的
11.“日管控情况汇总”柱图上面有一段需要删除的数据

## 要求

1.按照上面的问题逐一改动，每完成一步提交git 2.改动后请在这个文档里说明改动了什么，改动的理由是什么，改动的结果是什么3.改动前如果有疑问的，问清楚再改动4.尽量少改动，复制粘贴代码逻辑最好

## 改动记录

### 第1步

- 改动内容：移除了“采购总成本分析”环图的随机刷新定时器，保留原有固定数据和现有动画表现。
- 改动理由：这个模块每 5 秒会随机改值，和当前已经确认的校园采购成本数据冲突，会把改好的内容刷回去。
- 改动结果：环图现在固定展示 `蔬菜成本 30.2 / 肉类成本 52.3 / 主食成本 8.9 / 副食成本 8.6`，不会再自动乱跳。

### 第2步

- 改动内容：调整了“膳食经费数据分析”环形图的中心点、半径和引导线长度，让图形整体缩小并向左收。
- 改动理由：原图半径偏大，右侧图例与环图标签互相挤压，阅读性差。
- 改动结果：环图与右侧图例的重叠被压缩到最小，布局更接近目标图，同时没有改动这个区域的整体结构。

### 第3步

- 改动内容：统一替换了“日管控情况汇总”柱图中残留的通用文本，把图例和坐标轴名称改成当前业务口径。
- 改动理由：虽然数据已经切到日管控场景，但图上仍有“时间”“数量”这类通用词，容易让人误以为还是模板原图。
- 改动结果：柱图现在显示为 `排查总量 / 已完成 / 待整改`，坐标轴改为 `巡检时段 / 排查单位数`，页面语义和上方数据中心保持一致。

### 第4步

- 改动内容：把“消费数据分析”从原模板的牧草价格波动折线图替换成 4 个县的年度消费趋势图，并补上对应的月份、图例和提示交互。
- 改动理由：这块之前仍然沿用原模板业务口径，和“区域数据分布”里的大盘走势重复，内容语义不对。
- 改动结果：现在底部图表展示 `01县 / 02县 / 03县 / 04县` 的 12 个月消费趋势，鼠标移入时会按月份显示各县数值，和当前页面目标一致。

### 第5步

- 改动内容：修正了“食材验收质量分析”左侧条块的换算逻辑，把条块数量改为按百分比映射，并补上 `%` 展示。
- 改动理由：原来沿用了旧模板的 650 总量算法，导致 `25 / 60 / 15 / 0` 这种占比数据在左侧条块里显示失真。
- 改动结果：左侧条块与饼图占比现在一一对应，`25% / 60% / 15% / 0%` 的视觉比例能对上。

### 第6步

- 改动内容：把“学生营养情况分析”堆叠条上的数值标签统一改到各自颜色条块内部显示。
- 改动理由：原来只有部分系列显示数字，而且位置偏上，和用户要求的“数字在每个不同颜色条形图的中心”不一致。
- 改动结果：`副食 / 生鲜 / 粮油` 三段条块现在都会在对应色块中间显示各自数值，读取更直接。

### 第7步

- 改动内容：把“供应商评分分析”中 5 个供应商的评分值改成了不同分数。
- 改动理由：原来所有供应商都显示同一个分值，无法体现对比，也不符合用户这一步的要求。
- 改动结果：当前评分调整为 `A供 96 / B供 92 / C供 88 / D供 84 / E供 79`，图上可以直接看出差异。

### 第8步

- 改动内容：把“月调度情况汇总”滚动列表的渲染改成先清空旧内容，再固定按前 12 条月份文案生成。
- 改动理由：这个模块要求就是固定展示一月到十二月的 12 条月调度情况，需要避免被旧列表内容叠加或污染。
- 改动结果：右下滚动区现在稳定显示 `一月月调度情况` 到 `十二月月调度情况` 共 12 条，不会混入其它历史列表项。
